

<!-- First Grid -->
<div class="w3-row-padding w3-padding-64 w3-container">

 <section class="layout">
        <% @data.each do |item| %>
            <div class="dog">
              <img src="<%=item.image%>" alt="A dog photograph" width="400" height="350"><br>
              <%= link_to item.name, item %>
            </div>
        <% end %>
  </section>
  </div>

